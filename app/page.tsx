import { Button } from "@/components/ui/button";

const page = () => {
  return (
    <div>
      <Button variant={"secondary"} size={"lg"}>
        Click Me
      </Button>
    </div>
  );
};

export default page;
